const databaseName = '@gameplay';

const COLLECTION_USER = `${databaseName}:user`;
const COLLECTION_APPOINTMENTS = `${databaseName}:appointments`;

export { COLLECTION_USER, COLLECTION_APPOINTMENTS };
